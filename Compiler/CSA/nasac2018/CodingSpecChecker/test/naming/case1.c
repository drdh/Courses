// Words
int an_identifier;
int AlsoAnIdentifier;
int yetAnotherIdentifier;
int devices;

// Abbrs
char buf[2048];
int dev;
void cmp(int a, int b);
int init() {
    return 0;
}

// Words that should be suggested abbr
float maximum;
char message[1024];
int clock;
void report_error(char *error_content);
char *get_command_from_file();

// Words with typos
char mesage[1024];
void say_hekko();
void initializ();
int find_minimun(int array[], int len);

int main() {
    return 0;
}
