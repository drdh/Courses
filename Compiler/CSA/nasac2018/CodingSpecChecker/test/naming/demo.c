typedef struct {
    char *name;
    unsigned char *buffer;  // Suggest abbr for `buffer`
} device_t;

device_t *device_list;

void copy_buffer(device_t *destination, device_t *src) {  // Suggest abbr for `destination`
    char *buffer1, *buffer2;
    char __buffer3;
    char tmp_buffer; // TODO
}

void initializ_all_devices() {  // Warn `initializ` not a word
    unsigned int address;  // Suggest abbr for `address`
}

void InitializAllDivices() {
    unsigned int addess;  // Suggest abbr for `address`
}
